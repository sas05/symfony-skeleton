{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_stock_new' %}

{% block main %}
    <h1>{{ 'title.stock_new'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.company) }}
        {{ form_row(form.exchangeMarket) }}
        {{ form_row(form.type) }}
        {{ form_row(form.price) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'label.create_stock'|trans }}
        </button>
        {{ form_widget(form.saveAndCreateNew, {label: 'label.save_and_create_new', attr: {class: 'btn btn-primary'}}) }}
        <a href="{{ path('admin_dashboard_index') }}" class="btn btn-link">
            <i class="fa fa-list-alt" aria-hidden="true"></i> {{ 'action.back_to_list'|trans }}
        </a>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
